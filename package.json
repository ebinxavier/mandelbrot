{
  "homepage": "https://ebinxavier.github.io/mandelbrot",
  "name": "mandelbrot",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@assemblyscript/loader": "^0.18.16",
    "@testing-library/jest-dom": "^5.11.4",
    "@testing-library/react": "^11.1.0",
    "@testing-library/user-event": "^12.1.10",
    "as-bind": "^0.6.1",
    "lodash": "^4.17.21",
    "mathjs": "^9.3.0",
    "react": "^17.0.2",
    "react-dom": "^17.0.2",
    "react-scripts": "4.0.3",
    "tinygradient": "^1.1.4",
    "web-vitals": "^1.0.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build:react": "react-scripts build",
    "build:wasm": "asc ./node_modules/as-bind/lib/assembly/as-bind.ts assembly/index.ts -b public/fractal.wasm --optimize --exportRuntime",
    "build": "npm run build:wasm && npm run build:react",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "assemblyscript": "^0.18.16",
    "gh-pages": "^3.1.0"
  }
}
